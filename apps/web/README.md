# DeluluScore Web

Standalone Vite + React prototype for the DeluluScore idea evaluator.

## Run locally

```bash
npm install
npm run dev
```

This first slice uses deterministic local demo scoring. It does not call the API or claim to make trained-model predictions.
